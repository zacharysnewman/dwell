# shared/protocol

Protocol definitions shared by the C++ server core and the TypeScript client: message layouts
(ARCHITECTURE.md §8.3), shared constants (§7.4, `PLAYER_CONTROLLER.md` §7), and golden-byte test
vectors that both encoders/decoders must pass. Populated in Phase 1.
